<resources>
    <!-- Base application theme. -->
    <style name="Theme.MinhaReceita" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowSplashScreenBackground">@color/light_green</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_food</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_green</item>
        <item name="colorPrimaryVariant">@color/light_white</item>
        <item name="colorOnPrimary">@color/light_green</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
    </style>
</resources>